# 任务清单（进度记录）

> 最后更新：2026-08-20（打分机制 + 文档完善 + 主页测试）

---

## ① 已完成

### V1（单标签意图架构）—— 已完成并保留
- 三大类 8 标签意图识别 + 多意图数组 + 易混淆边界规则
- 多意图端到端、缺参数强指令、防幻觉铁律、SAFETY_RULES
- 三大意图增强：time（承诺偏差/准时率/可预测性）、risk（跨品类/差评原因）、cost（性价比评分/运费/基线）
- recommend、数据治理踩坑修复、双栏 UI（app.py）
- 详见 git 历史 commit（5cfd3d4 及之前）

### V2.0（三元组架构）—— 已完成 ✅

**架构**
- 三元组识别：operation（query/compare/aggregate/recommend）× dimension（seller/category/route）× metric（8 个指标）
- 数据映射表 `QUERY_DISPATCH` + 参数必填规则 `REQUIRED_PARAMS`（加能力 = 加一行）

**四个操作全部实现**
- query / compare（多对象循环，dispatch 层统一）/ aggregate（排名）/ recommend（筛卖家+拼时效）

**数据**
- 新增 `route_freight`、`category_freight` 两张表；`category_ship_time` 已废弃（发货是商家行为，品类是伪命题）
- `query_v2.py`：V2 聚合查询层

**网页**
- `app_v2.py`：V2 网页，与 V1 并存

**语气人设**：柔和亲切俏皮

**bug 修复（8 个，全部验收）**
1. sort_direction 语义（按指标分默认方向）
2. route×freight 漏映射
3. recommend 数据瘦（补 city/state + 时效字段）
4. 多对象循环对比（dispatch 层通用逻辑）
5. total_time vs transit_time 边界
6. 防幻觉强化
7. 品类发货删除（ship_time 只留 seller，品类引导回卖家）
8. 拆段回答（total_time 拆发货+运输+总计）

### Eval 评测工程 —— 已完成 ✅
- `eval/cases.jsonl`（31 条用例）+ `eval/eval.py`（自动判分）
- **31/31 通过率 100%**（防复发 8 + 能力矩阵 16 + 边界 7）
- 三重判分：意图匹配 + 禁止词 + 必须词
- 每次改代码后跑 Eval 防回归

### 运营后台（pages/admin.py）—— 已完成 ✅（四大模块 + 双层评审）
- **① 指标看板**：Agent+模型双看板（KPI、调用趋势、意图分布、Eval 趋势）
- **② 模型管理**：主模型/裁判模型可换 + token 统计 + 成本估算 + 明细表
- **③ Agent 管理**：干预开关（功能级+州/品类黑名单）+ 提示词调优（版本回滚 + AB 测试）
- **④ 测评管理**：Eval 中心（用例可编辑/增删）+ 坏例闭环（标坏例→进 Eval+BUG单）+ BUG 反馈
- **双层评审**：框架评审（代码规则）+ 模型评审（裁判模型 v4-pro 打分），写 logs/evaluations.jsonl
- 前端 app_v2.py ↔ 后台互跳（st.switch_page）

### 模型配置（实测确认）—— 已完成 ✅
- 主模型 `deepseek-chat`（对话模式，快省稳，非别名）
- 裁判模型 `deepseek-v4-pro`（推理，评审用）
- max_tokens 全部 2000（防推理模型思考吃光）

### 部署上线（08-16 ~ 08-19）—— 已完成 ✅
- **Supabase 数据库接入**：5 张表（conversations/evaluations/cases/bug_feedback/prompt_versions + token_usage），数据持久化、多人共享
- **Streamlit Cloud 部署**：助手 + 后台上线，公网访问
- **Eval 改读 Supabase + 并发提速**：32 条 20 秒跑完（5.4×）
- **坏例闭环打通**：标记坏例 → 规范化（normalize_case）→ 进 Eval → 参与测评
- **双层评审完成**：裁判带数据依据核对（accuracy=9，不再误判）
- **token 持久化**：写 Supabase
- **模型配置驱动**：chat 每次重读配置，跨进程生效

### 后台打磨（08-16 ~ 08-19）—— 已完成 ✅
- **指标看板修复**：token K 单位、Eval 通过率取最新 run、调用量日期统一、走势图只取 Eval run
- **Eval 异步后台跑**：不阻塞请求、不超时
- **干预开关改下拉框**：中文备注，不再手输英文
- **坏例流程**：标记填原因+期望意图、评审按时间匹配、时间显示北京时间
- **模型管理布局**：主模型/裁判模型分列，温度/max_tokens 对齐

### 文档整理（08-19）—— 已完成 ✅
- **飞书主文档**（作品集）：作品展示 + 背景/竞品/用户分析 + 核心链路 + 技术架构 + 测评逻辑 + 运营后台
- **飞书提示词文档**：10 个真实提示词 + 时序流程图
- **竞品调研报告**（competitive-analysis-full.md）：覆盖 6 个分析角度
- **测试说明书**（notes/测试说明书.md）：发给朋友测试用

---

## ② 未完成

**能力边界（2026-08-21 归档）**
0. **「某品类有几个卖家」→ 归 unsupported**：已实测 5 种问法，识别不稳定（有的归 aggregate×price/freight 查不到，有的归 unsupported）。已在 [业务边界.md](业务边界.md) 归档能力清单（18 组合）与边界分类。待办：①意图分类提示词加边界规则（需产品确认）；②补 Eval 边界用例；③暂不建"卖家数量"数据（不在决策链上）。

**补数据（1 项，P3）**
1. **category × ontime_rate / promise_gap**（品类准时率/承诺偏差）

**面试相关（P1）**
2. **用户分析章节**（飞书文档第三大点，待补内容）
3. **面试话术打磨**：能把项目讲透（五条线：竞品/用户/核心链路/评估/技术边界）
4. **发朋友测试**：测试说明书已备好，多人协作跑起来

**长期**
5. **AI 个人独立站**：单独节奏做

---

## ③ 下一步（按顺序）

1. **补飞书用户分析章节** + 面试话术打磨 —— P1
2. **发朋友测试** —— P1（验证真实使用）
3. 补数据 category×准时/偏差 —— P3

---

## ④ 注意事项（踩过的坑，务必记住）

### 数据层
1. 类目命名统一用英语（office_furniture 不是 moveis_escritorio）
2. 差评率分母 = 差评数÷评论数（不是÷订单数）
3. 多对多归属：评价订单级，一单多卖家要卖家级聚合
4. 卖家缩写短前缀匹配多个 → 诚实说"不唯一"，不静默取第一个
5. 结论方向：数字对还不够，谁高谁低方向也要对
6. 数字三方对齐：故事/脚本/知识库三处一致
7. **cat_avg_return_kw ≠ 差评基线**（那是退货词均值，差评基线用 category_baseline.neg_rate）
8. **卖家 ID 不是卖家州**（query_timing 传 seller_id 永远匹配不到，先 query_seller_state 反查）

### 评分与口径
9. **min-max 归一化抹平绝对差距** → 用相对比例 score = min/value
10. **neg_rate 双口径**（seller_risk vs seller_cost），评分统一从 seller_risk 取
11. 卖家级差评原因样本稀疏 → 诚实标注或回退品类，不编造

### V2.0 新增踩坑
12. **sort_direction 不能一刀切**：ship_time 类"排名"默认 asc（最快），freight/price/neg_rate 默认 desc（最贵/最差），回答方向必须和排序一致
13. **能力矩阵格子要两操作都做**：route×freight 做了 aggregate 漏 query，导致"MS送到SP运费"查不到
14. **recommend 数据要丰富**：只返回哈希 ID 会让 LLM 空谈 + 编造，必须补 seller_city/seller_state
15. **多对象循环是通用逻辑**：多卖家只查第一个是打补丁，应在 dispatch 层统一"多值→循环对比"，覆盖所有指标和维度
16. **capability 约束要写死**：软约束"不要编造"会被 LLM 忽略，必须明确"只能文字、只能 4 项能力、禁止读链接截图"

### 防幻觉铁律（贯穿 V1 和 V2）
17. 数字不让 LLM 算，由数据层查
18. 缺失维度禁编造（没查价格不说"综合推荐"，没查时效不编天数）
19. 只答用户问的指标，不脑补没问的维度
20. 数据模糊化（约/大概/左右/九成），禁 n=/P50/P90

### 数据边界与安全
21. 数据 2016-2018 巴西数据，标注"用于验证方法论"
22. 无实时数据，标"基于历史数据仅供参考"
23. 无长期记忆（会话内 5 轮）
24. API key 不进 git（.env 已忽略）
25. 特殊品类约束（美妆/母婴/医疗器械/食品/二手）

### 架构认知（用户领悟的关键）
26. **不是 RAG，是结构化查询 + 手工版 function calling**（数据是精确数值，不是文本；LLM 分类 + 代码路由，不是 LLM 自主选函数）
27. **prompt + LLM 语义理解**：框架写死（有哪些三元组），判断靠理解（这句话归哪个）
28. **核心从业务逻辑出发**：建数据 = 用户决策链上缺什么信息，不是"数据能算什么"
